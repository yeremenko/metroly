define([
	'underscore',
	'backbone'
], function(_, Backbone) {

	var BusLine = Backbone.Model.extend({
        defaults: {

        },

        initialize: function() {

        },

    });
	return BusLine;
});